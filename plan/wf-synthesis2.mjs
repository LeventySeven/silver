export const meta = {
  name: 'silver-synthesis2',
  description: 'Deep synthesis II: top-5 dominance per source + empirical engine efficiency vs Vercel + Anthropic skill best-practices + transcripts/BAD_GUIDE + gap map -> adopt-list + world-class SKILL design -> red-team.',
  phases: [
    { title: 'Investigate', detail: 'top-5-per-source, deep engine dives, empirical measurement, Anthropic skill patterns, transcripts/BAD_GUIDE, Silver gap map' },
    { title: 'Decide', detail: 'synthesize the adopt-list (engine + top-5 gaps) + the world-class SKILL design' },
    { title: 'RedTeam', detail: 'critical-thinking: cargo-cult / keyless / cost-benefit / SKILL over-promise' },
  ],
}

const REPO = '/Users/seventyleven/Desktop/Silver'
const OUT = REPO + '/research/topfive'
const SRC = REPO + '/silver/src'
const REF = REPO + '/reference'
const ORACLE = REPO + '/rust-oracle'         // = Vercel agent-browser's real Rust code
const RF = '/Users/seventyleven/Desktop/researchfms'
const TD = RF + '/teardowns'
const R1 = REPO + '/research/sources'
const SILVER_BIN = REPO + '/silver/dist/cli.js'
const CHROME = '/Users/seventyleven/.agent-browser/browsers/chrome-149.0.7827.54/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing'

const CTX = 'PROJECT "Silver": a keyless Node/TS browser CLI on Playwright (src at ' + SRC + '; binary `node ' + SILVER_BIN + '`). Host LLM is the brain; Silver NEVER calls a model. It already synthesizes Vercel agent-browser + Aside + Webwright + browser-use + Stagehand + AgentQL into one CLI (full verb parity + tasks + subagents + memory + parallel + extract + security, 230 tests, eval-gated). GOAL of this pass: make Silver GENUINELY better than each source by taking the TOP capabilities of each — true synthesis, not feature-checkboxing — AND verify/close the ENGINE efficiency gap vs Vercel (Vercel is Rust with a persistent daemon; Silver is TS with per-command CDP reconnect). Token-efficiency = a property of the SNAPSHOT format (not the language); latency = affected by the connection model. We must be honest + empirical.'

const TOOLS = 'You have FULL tool access (Read, Grep, Glob, Bash, WebFetch, WebSearch). READ real code/teardowns; ground every claim in a file you read. Be objective + specific.'

const INV = [
  { slug: 'top5-vercel', model: 'sonnet', focus: 'Read Vercel agent-browser\'s REAL code at ' + ORACLE + '/cli/src (+ ' + TD + '/VERCEL.md). Identify the TOP 5 things Vercel does BETTER than every competitor (esp. its ENGINE: the persistent per-session daemon holding one CDP connection, the @eN snapshot compactness, speed, the diff snapshot, the docs-in-binary two-tier skill, session/restore). For EACH: what it is, why it wins, the file:line, and whether Silver (' + SRC + ') already has it or has a GAP. Write ' + OUT + '/top5-vercel.md.' },
  { slug: 'top5-aside', model: 'sonnet', focus: 'Read the Aside teardown (' + R1 + '/aside-*.md + ' + TD + '/_aside_parts). TOP 5 things Aside does BETTER than every competitor (why-SOTA harness, the REPL/code-execution tool, long-horizon loop discipline, memory/dreaming, subagents, the compact diffed a11y snapshot, security). For each: what/why/evidence + does Silver have it or GAP. Write ' + OUT + '/top5-aside.md.' },
  { slug: 'top5-webwright', model: 'sonnet', focus: 'Read Webwright\'s REAL code at ' + REF + '/webwright/src + skills. TOP 5 things Webwright does BETTER (esp. HOW it opens + PERSISTS the browser — read tools/persistent_local_browser.py, environments/local_browser.py — and how it handles LONG tasks / re-runnable script artifact / the skill form). For each: what/why/file:line + does Silver have it or GAP. Write ' + OUT + '/top5-webwright.md.' },
  { slug: 'top5-browseruse', model: 'sonnet', focus: 'Read browser-use REAL code at ' + REF + '/browser-use/browser_use (+ ' + R1 + '/r2-browseruse-*.md). TOP 5 things browser-use does BETTER (DOM serializer / interactive-element cascade, the action-registry decorator, multi_act page-change guard, watchdogs, sensitive-data). For each: what/why/file:line + Silver has it or GAP. Write ' + OUT + '/top5-browseruse.md.' },
  { slug: 'top5-stagehand', model: 'sonnet', focus: 'Read Stagehand REAL code at ' + REF + '/stagehand (+ ' + R1 + '/r2-stagehand-*.md). TOP 5 things Stagehand does BETTER (act/observe/extract, id-grounding, self-heal/caching, the a11y tree). For each: what/why/file:line + Silver has it or GAP. Write ' + OUT + '/top5-stagehand.md.' },
  { slug: 'top5-agentql', model: 'sonnet', focus: 'Read AgentQL teardown (' + RF + '/agentql + ' + R1 + '/agentql.md). TOP 5 things AgentQL does BETTER (resilient query resolution, deterministic-before-model fallback, query caching, browser fleet). For each: what/why/evidence + Silver has it or GAP (avoid a full DSL — flagged anti-pattern). Write ' + OUT + '/top5-agentql.md.' },
  { slug: 'top5-perplexity', model: 'sonnet', focus: 'Read the Perplexity Computer teardown (' + TD + '/PERPLEXITY_COMPUTER.md). TOP 5 things Perplexity/Comet does BETTER (BrowseSafe injection defense, task decomposition, verification, vision-gating). For each: what/why/evidence + Silver has it (keyless subset) or GAP. Write ' + OUT + '/top5-perplexity.md.' },

  { slug: 'engine-vercel-deep', model: 'opus', focus: 'DEEP ENGINE DIVE on Vercel (' + ORACLE + '/cli/src, esp. connection.rs / daemon.rs / native/{browser,snapshot,cdp}.rs). Exactly HOW does Vercel achieve low latency + token-efficiency? The persistent daemon + one held CDP connection (no per-command reconnect); the snapshot compaction (compact/interactive/depth); connection warmth; what makes it FAST + LEAN. Then compare PRECISELY to Silver: Silver spawns detached Chromium + connectOverCDP EVERY command (' + SRC + '/core/session.ts) — quantify the reconnect cost and design the concrete Silver improvement (a persistent connection/daemon or connection-reuse) that closes the latency gap WITHOUT breaking the stateless-command ergonomics or keyless-ness. Write ' + OUT + '/engine-vercel-deep.md with a concrete Silver engine plan.' },
  { slug: 'engine-webwright-deep', model: 'opus', focus: 'DEEP DIVE on Webwright\'s browser lifecycle + long-task engine (' + REF + '/webwright/src/tools/persistent_local_browser.py, environments/local_browser.py, run/*, agents/default.py). Exactly HOW does it open + persist the browser across a long task, checkpoint, and resume? What is genuinely superior to Silver\'s task run-folder + session model? Concrete adopt list for Silver (keyless). Write ' + OUT + '/engine-webwright-deep.md.' },
  { slug: 'measure-efficiency', model: 'opus', focus: 'EMPIRICAL MEASUREMENT. Vercel agent-browser 0.31.2 is on PATH; Silver is `node ' + SILVER_BIN + '`. On the SAME set of real pages (https://example.com, https://news.ycombinator.com, https://en.wikipedia.org/wiki/Web_browser), MEASURE and TABULATE: (1) snapshot -i output SIZE in chars (proxy for tokens) for Silver vs Vercel — who is leaner? (2) per-command LATENCY (time `open` then 3x `snapshot`) for Silver vs Vercel — quantify the reconnect overhead. Use Silver with --executable-path if needed (Chrome at "' + CHROME + '") or its own Playwright chromium; use vercel `agent-browser` normally. Run each a few times, report medians. Be rigorous and HONEST — if Silver is already competitive on tokens, say so; if it is slower per-command, quantify by how much. Write ' + OUT + '/measure-efficiency.md with the real numbers + the verdict on whether an engine change is warranted.' },

  { slug: 'anthropic-skill-patterns', model: 'sonnet', focus: 'Find ALL of Anthropic\'s official best practices for authoring Agent Skills / SKILL.md. WebSearch + WebFetch Anthropic docs (docs.claude.com / anthropic.com engineering blog: "Agent Skills", "skill authoring best practices", "equipping agents for the real world with agent skills", the skill-creator guidance). ALSO read the on-disk skill conventions: ' + REPO + '/../compound-v/skills (frontmatter, structure) and Silver\'s current ' + REPO + '/silver/skill-data/core/SKILL.md. Extract the DEFINITIVE checklist: frontmatter (name/description trigger-phrasing), progressive disclosure (SKILL.md + linked references + scripts), when-to-use triggers, concrete examples, keeping it concise, tool/command tables, the "clear contract" principle, degradation. Write ' + OUT + '/anthropic-skill-patterns.md as a build-ready checklist for Silver\'s SKILL.' },
  { slug: 'transcripts-badguide', model: 'sonnet', focus: 'Mine /Users/seventyleven/Desktop/BAD_GUIDE.md and ' + RF + '/Transcripts/ (esp. TRANSCRIPTS_ANTHROPIC.md, TRANSCRIPTS_CLAUDE_CODE_101.md, and any about agents/browsers/skills/tools). Extract: (a) skill-authoring + agent-usage wisdom applicable to Silver\'s SKILL; (b) GENERALIZED browser-agent USE CASES beyond "Vercel=quick, Aside+Webwright=long" (e.g. form-filling, research, monitoring, extraction pipelines, auth flows, testing, scraping-with-consent, multi-tab parallel gather) — a broad taxonomy of what agents actually do with a browser, to make Silver\'s SKILL usage-case-rich + generalized. Write ' + OUT + '/transcripts-badguide.md.' },
  { slug: 'silver-gap-selfmap', model: 'opus', focus: 'Read ALL of Silver\'s current code (' + SRC + ') + its SKILL (' + REPO + '/silver/skill-data/core/SKILL.md) + evals (' + REPO + '/evals). Produce an honest GAP MAP: for the capability areas (perception/token-efficiency, actuation, extract, long-tasks, parallel/subagents, memory, security, session/engine latency, skill/docs), what does Silver ALREADY do well, and what are its real WEAKNESSES vs the best-in-class from the other sources? This is Silver\'s self-critique to focus the adopt-list. Write ' + OUT + '/silver-gap-selfmap.md.' },
]

phase('Investigate')
const inv = await parallel(INV.map((a) => () =>
  agent(TOOLS + '\n\n' + CTX + '\n\nTASK (' + a.slug + '): ' + a.focus + '\n\nReturn a <=300-word summary + the digest path. Be concrete, cite files, and clearly separate "Silver already has this" from "GAP: Silver should adopt X".',
    { label: a.slug, phase: 'Investigate', model: a.model, effort: a.model === 'opus' ? 'high' : 'medium' }
  ).then((r) => ({ slug: a.slug, r })).catch(() => null)
))
log('Investigate: ' + inv.filter(Boolean).length + '/' + INV.length + ' done.')

phase('Decide')
const adoptPath = REPO + '/research/synthesis/adopt-list-and-skill.md'
const decide = await agent(TOOLS + '\n\n' + CTX + '\n\nYou are the SYNTHESIS lead. Read ALL digests under ' + OUT + '/ (ls then read every .md) — the top-5-per-source, the two deep engine dives, the EMPIRICAL measurement, the Anthropic skill-patterns checklist, the transcripts/BAD_GUIDE usage taxonomy, and Silver\'s gap self-map.\n\nWrite ' + adoptPath + ' — the decisive, prioritized ADOPT-LIST + SKILL DESIGN:\n1. **ENGINE** — based on the EMPIRICAL numbers: is Silver token-competitive with Vercel (keep/improve the snapshot)? Is the per-command latency gap worth closing with a persistent connection/daemon (concrete design: e.g. a long-lived CDP connection cached across commands, or an opt-in daemon), and is it keyless + worth the complexity? Decide + specify.\n2. **TOP-5 GAPS TO ADOPT** — merge the per-source top-5s; list ONLY the concrete capabilities Silver LACKS or does worse, each mapped to a Silver file/change, keyless, prioritized P0/P1/P2. Skip anything Silver already does well or that is cargo-cult.\n3. **THE WORLD-CLASS SKILL** — a complete spec for the new SKILL.md following the Anthropic best-practices checklist: frontmatter, progressive disclosure (SKILL.md + reference files + examples), when-to-use triggers, a GENERALIZED use-case taxonomy (many cases, not just quick-vs-long), decision guidance (when to use which verb/mode), and copy-ready structure. This must make ANY agent 100% understand what Silver is + how to use it fully.\nBe decisive; cite sources. Return a <=450-word exec summary + the path.',
  { label: 'decide', phase: 'Decide', effort: 'high' })

phase('RedTeam')
const rtPath = REPO + '/research/synthesis/adopt-list-redteam.md'
const rt = await agent(TOOLS + '\n\nAdversarial critic (Compound-V critical-thinking). Read ' + adoptPath + ' + the digests under ' + OUT + '/ + Silver src (' + SRC + '). Attack the adopt-list: (a) Is the ENGINE change (persistent connection/daemon) actually justified by the measured numbers, or premature optimization that risks the robust stateless model + adds concurrency bugs? Steelman keeping the reconnect model. (b) Which "top-5 gaps" are cargo-cult / already-covered / non-keyless (flag any that need a model call)? (c) Does the SKILL design over-promise or bloat (a skill that is too long is an anti-pattern)? (d) The token-efficiency claim — do the measured numbers actually support the plan? Write ' + rtPath + ': confirm-or-cut each item, the strongest counter-case, and the corrected priority. Blunt, no praise-padding. Return summary + path.',
  { label: 'redteam', phase: 'RedTeam', effort: 'high' })

return { investigated: inv.filter(Boolean).length, adoptPath, decide, rtPath, rt }
