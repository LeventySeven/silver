{
  "name": "silver-agent-browser",
  "version": "0.1.0",
  "description": "Silver — the keyless browser for AI agents (CLI + skill).",
  "bin": { "silver": "silver/dist/cli.js" },
  "engines": { "node": ">=24" },
  "scripts": { "prepare": "npm --prefix silver install --no-audit --no-fund && npm --prefix silver run build" },
  "license": "MIT"
}
